/*
 * @flow
 */

const ROOT :'/' = '/';

const ACCESS_REQUEST_ID_PARAM :':accessRequestId' = ':accessRequestId';
const ACCESS_REQUEST_PATH :string = `/request/${ACCESS_REQUEST_ID_PARAM}`;

const NEW :'/new' = '/new';

export {
  ACCESS_REQUEST_ID_PARAM,
  ACCESS_REQUEST_PATH,
  NEW,
  ROOT,
};
