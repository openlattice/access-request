/*
 * @flow
 */

const ROOT :'/' = '/';

const ACCESS_ID_PARAM :string = ':accessId';
const ACCESS_REQUEST_PATH :string = `/request/${ACCESS_ID_PARAM}`;

const NEW_REQUEST_PATH :string = '/new';

export {
  ACCESS_ID_PARAM,
  ACCESS_REQUEST_PATH,
  NEW_REQUEST_PATH,
  ROOT,
};
